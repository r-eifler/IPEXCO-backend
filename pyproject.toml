[project]
name = "ipexco-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["setuptools", "wheel","unified-planning]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
